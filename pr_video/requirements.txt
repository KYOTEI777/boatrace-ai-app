# PR動画生成 依存ライブラリ

# ── 無料モード（--free-mode）で必要 ──────────────────────────
# 音声生成（日本語TTS、無料・APIキー不要）
edge-tts>=6.1.9

# 動画編集・Ken Burns効果
moviepy>=1.0.3
imageio>=2.34.0
imageio-ffmpeg>=0.4.9
Pillow>=10.0.0

# ── 本格モード（通常）で追加で必要 ───────────────────────────
# 動画生成API（Runway Gen-4 Turbo）
runwayml>=0.1.0

# リップシンクAPI（Replicate）
replicate>=0.30.0

# ── 共通ユーティリティ ────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
numpy>=1.24.0
